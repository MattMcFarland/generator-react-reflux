var Reflux = require('reflux');

module.exports = Reflux.createActions([
  // use yo rux:add <action> <%= lodash.capitalize(path.basename(actionsName)) %> <store> to register a new action to this file and the store file.
]);


